# 国信合创理事会小程序 - 项目文档

## 📋 项目概述

**项目名称**：国信合创理事会  
**技术框架**：uni-app (Vue 3)  
**项目类型**：微信小程序  
**版本**：1.0.0

这是一个面向投资和创业领域的综合性小程序平台，主要服务于理事会成员、创业者、投资人和校园合伙人，提供BP诊断、项目孵化、投资分析等核心功能。

---

## 🏗️ 项目架构

### 技术栈
- **框架**：uni-app (支持多端：微信小程序、H5、APP)
- **Vue版本**：Vue 3
- **状态管理**：Vuex
- **HTTP请求**：基于 uni.request 封装的 request 工具
- **UI组件**：自定义组件 + uni-ui 组件库

### 目录结构

```
gxhc-mini/
├── api/                    # API接口定义
│   ├── api.js             # 通用API（首页、登录、优惠券等）
│   ├── gxhc.js            # 核心业务API（理事、项目、BP诊断等）
│   ├── public.js          # 公共API（微信授权、配置等）
│   ├── user.js            # 用户相关API
│   ├── order.js           # 订单相关API
│   ├── kefu.js            # 客服相关API
│   └── game.js            # 游戏相关API
├── pages/                  # 主包页面
│   ├── home/              # 首页模块
│   ├── domain/            # 功能页
│   ├── mine/              # 我的/个人中心
│   ├── director/          # 理事成员
│   ├── project/           # 项目相关
│   ├── points/            # 积分系统
│   └── users/             # 用户相关（发票、推广等）
├── subpackage1/            # 分包1：核心功能
│   ├── bp/                # BP诊断相关
│   ├── agent/             # 投资分析Agent
│   ├── energy/            # 能量系统
│   ├── auth/              # 认证相关
│   └── kf/                # 客服
├── campusPages/            # 分包：校园合伙人
│   ├── parter/            # 合伙人申请/列表
│   ├── activity/          # 活动管理
│   └── experience/        # 经验值系统
├── projectPages/           # 分包：项目相关
│   ├── list/              # 项目列表
│   ├── memberInfo/        # 成员信息
│   ├── workExperience/    # 工作经历
│   ├── eduExperience/     # 教育经历
│   ├── practiceExperience/# 实习经历
│   ├── hatchList/         # 孵化项目
│   └── updateBP/          # 更新BP
├── managePages/            # 分包：管理员功能
│   ├── index/             # 管理员操作中心
│   ├── director/          # 添加理事成员
│   ├── point/             # 积分登记
│   ├── users/             # 用户管理
│   └── banner/            # 轮播图管理
├── explainPages/           # 分包：说明页面
│   ├── project/           # 项目发起说明
│   ├── protocol/          # 协议说明
│   ├── campus/            # 校园合伙人介绍
│   ├── points/            # 积分说明
│   └── news/              # 新闻资讯
├── components/             # 公共组件
│   ├── x-navbar/          # 自定义导航栏
│   ├── x-popup/           # 弹窗组件
│   ├── x-empty/           # 空状态组件
│   ├── x-load-more/       # 加载更多
│   ├── mp-html/           # HTML渲染组件
│   └── project-detail/    # 项目详情组件
├── store/                  # Vuex状态管理
│   ├── modules/           # 模块化store
│   │   ├── app.js         # 应用状态（登录、用户信息）
│   │   ├── indexData.js   # 首页数据
│   │   └── hotWords.js    # 热词
│   └── getters.js         # getters
├── utils/                  # 工具函数
│   ├── request.js         # HTTP请求封装
│   ├── cache.js           # 缓存工具
│   ├── util.js            # 通用工具
│   └── formatDate.js      # 日期格式化
├── config/                 # 配置文件
│   ├── app.js             # 应用配置（API地址、请求头等）
│   ├── cache.js           # 缓存key定义
│   └── const.js           # 常量定义
├── libs/                   # 第三方库
│   ├── login.js           # 登录相关
│   ├── wechat.js          # 微信相关
│   └── routine.js         # 小程序相关
├── static/                 # 静态资源
│   ├── images/            # 图片资源
│   ├── css/               # 样式文件
│   └── iconfont/          # 图标字体
├── cloud/                  # 云开发相关
│   └── miniprogram/       # 云存储文件
├── App.vue                 # 应用入口
├── main.js                 # 主入口文件
├── pages.json              # 页面路由配置
└── manifest.json           # 应用配置清单
```

---

## 🎯 核心功能模块

### 1. 首页模块 (`pages/home/`)

**主要功能**：
- 基金矩阵介绍
- 理事会介绍
- AI创诊报告入口（BP一键诊断、投资分析Agent）
- 智能投顾助手（上市公司服务AI、行业研究AI、专家BOT）
- 媒体矩阵展示
- 服务列表（VIP服务、普通服务）

**关键页面**：
- `pages/home/index/index.vue` - 首页
- `pages/home/introduce/index.vue` - 基金矩阵介绍
- `pages/home/serviceList/index.vue` - 服务列表
- `pages/home/vipServiceList/index.vue` - VIP资源列表

### 2. BP诊断模块 (`subpackage1/bp/`)

**主要功能**：
- BP一键诊断：上传商业计划书，AI自动分析
- BP诊断报告（Base版、Plus版）
- 一对一诊断直播申请
- BP上传记录
- 诊断样本案例
- 反馈功能

**关键API**：
- `runBp()` - 运行BP诊断
- `getBpInfo()` - 获取BP信息
- `getBpResultList()` - 获取诊断结果列表
- `getBpResultInfo()` - 获取诊断结果详情
- `applyLiveApply()` - 申请直播诊断

### 3. 投资分析Agent (`subpackage1/agent/`)

**主要功能**：
- 投资分析Agent对话
- 历史记录查看
- 投资报告生成

**关键页面**：
- `subpackage1/agent/index/index.vue` - Agent主页面
- `subpackage1/agent/list/index.vue` - 历史记录
- `subpackage1/agent/detail/index.vue` - 投资报告详情

### 4. 项目模块 (`pages/project/` + `projectPages/`)

**主要功能**：
- 项目发起
- 项目详情查看
- 项目进展跟踪
- 项目资料更新
- 商业计划书更新
- 财务信息更新
- 孵化项目列表
- 项目反馈

**关键API**：
- `investProjectsSave()` - 保存投资项目
- `investProjectsDetails()` - 获取项目详情
- `investProjectsUpdateBP()` - 更新BP
- `investProjectsUpdateSupply()` - 更新供需信息
- `investProjectsUpdateScore()` - 更新评分

**关键页面**：
- `pages/project/apply/index.vue` - 发起项目
- `pages/project/detail/index.vue` - 项目详情
- `pages/project/evolve/index.vue` - 项目进展
- `projectPages/list/index.vue` - 项目列表
- `projectPages/updateBP/index.vue` - 更新BP
- `projectPages/updateIndex/index.vue` - 项目资料更新

### 5. 理事成员模块 (`pages/director/`)

**主要功能**：
- 理事成员列表
- 成员详情查看
- 我的合同管理
- 添加理事成员（管理员）

**关键API**：
- `getDirectorMember()` - 获取理事成员列表
- `getDirectorDetails()` - 获取成员详情
- `directorMemberAdd()` - 添加成员（管理员）

**关键页面**：
- `pages/director/list/index.vue` - 理事成员列表
- `pages/director/detail/index.vue` - 成员简介
- `pages/director/contract/index.vue` - 我的合同

### 6. 积分系统 (`pages/points/`)

**主要功能**：
- 我的积分查看
- 积分排行榜
- 积分获取方式说明
- 积分管理制度

**关键页面**：
- `pages/points/detail/index.vue` - 我的积分
- `pages/points/ranking/index.vue` - 积分排行榜
- `explainPages/points/obtain/index.vue` - 积分获取说明
- `explainPages/points/ruleState/index.vue` - 积分管理制度

### 7. 能量系统 (`subpackage1/energy/`)

**主要功能**：
- 我的能量查看
- 能量明细记录

**关键API**：
- `userEnergy()` - 获取用户能量
- `energyList()` - 获取能量明细列表

### 8. 校园合伙人模块 (`campusPages/`)

**主要功能**：
- 校园大使申请
- 校园合伙人风采展示
- 活动报名与管理
- 活动详情
- 活动发起
- 经验值申报
- 经验值排行榜
- 经验值明细

**关键页面**：
- `campusPages/parter/apply/index.vue` - 校园大使申请
- `campusPages/parter/list/index.vue` - 校园合伙人风采
- `campusPages/activity/list/index.vue` - 热门活动
- `campusPages/activity/apply/index.vue` - 活动报名
- `campusPages/activity/detail/index.vue` - 活动详情
- `campusPages/experience/apply/index.vue` - 经验值申报
- `campusPages/experience/rankingList/index.vue` - 经验值排行榜

### 9. 用户中心 (`pages/mine/`)

**主要功能**：
- 用户登录/注册
- 个人信息管理
- 实名认证
- 客户中心
- 收件地址管理
- 消息中心
- 发票管理
- 推广名片

**关键页面**：
- `pages/mine/index/index.vue` - 我的主页
- `pages/mine/login/index.vue` - 登录页
- `pages/mine/customer/index/index.vue` - 客户中心
- `pages/mine/customer/personInfo/index.vue` - 个人信息
- `pages/mine/customer/realName/index.vue` - 实名认证
- `pages/mine/message/index.vue` - 消息中心
- `pages/users/user_invoice_list/index.vue` - 发票管理
- `pages/users/user_spread_code/index.vue` - 推广名片

### 10. 管理员功能 (`managePages/`)

**主要功能**：
- 管理员操作中心
- 添加理事会成员
- 积分登记
- 用户列表管理
- 上传合同
- 轮播图管理（添加/删除）

**关键页面**：
- `managePages/index/index.vue` - 管理员操作中心
- `managePages/director/addDirector/index.vue` - 添加理事成员
- `managePages/point/enter/index.vue` - 积分登记
- `managePages/users/index/index.vue` - 用户列表
- `managePages/users/contract/index.vue` - 上传合同

---

## 🔧 技术实现细节

### 1. 请求封装 (`utils/request.js`)

**特点**：
- 统一的请求拦截和响应处理
- 自动添加Token认证
- 登录状态检测和自动跳转
- 错误统一处理
- 支持 `noAuth` 参数跳过认证

**使用示例**：
```javascript
import request from "@/utils/request.js";

// GET请求
request.get('api/endpoint', { param: 'value' }, { noAuth: true });

// POST请求
request.post('api/endpoint', { data: 'value' });
```

### 2. 状态管理 (`store/`)

**主要状态**：
- `app.token` - 登录Token
- `app.userInfo` - 用户信息
- `app.uid` - 用户ID
- `app.phoneStatus` - 手机号绑定状态

**关键Mutation**：
- `LOGIN` - 登录，保存Token
- `LOGOUT` - 退出登录
- `UPDATE_USERINFO` - 更新用户信息

### 3. 配置管理 (`config/app.js`)

**关键配置**：
- `HTTP_REQUEST_URL` - API请求地址（根据环境变量切换）
- `env` - 环境变量（test/mini）
- `TOKENNAME` - Token请求头名称：`Authori-zation`
- `TIMEOUT` - 请求超时时间：10秒

**环境配置**：
```javascript
export const env = "test"; // 或 "mini"
export const HTTP_REQUEST_URL = `https://${env}.acture-hub.com`;
```

### 4. 路由配置 (`pages.json`)

**特点**：
- 主包包含核心页面（首页、功能、我的）
- 使用分包加载优化性能
- 自定义底部TabBar（首页、功能、我的）
- 支持自定义导航栏样式

**分包策略**：
- `subpackage1` - 核心功能（BP、Agent、能量等）
- `campusPages` - 校园合伙人相关
- `projectPages` - 项目相关
- `managePages` - 管理员功能
- `explainPages` - 说明页面
- `pages/users` - 用户相关

### 5. 组件系统

**自定义组件**（通过 `easycom` 自动引入）：
- `x-navbar` - 自定义导航栏
- `x-popup` - 弹窗组件
- `x-empty` - 空状态
- `x-load-more` - 加载更多
- `x-load-page` - 加载页面
- `x-empty-page` - 空页面

**使用方式**：
```vue
<x-navbar title="标题"></x-navbar>
<x-popup v-model="show"></x-popup>
```

---

## 🔐 认证与授权

### 登录流程

1. **小程序登录**：
   - 调用 `wx.login()` 获取 code
   - 调用 `login()` 或 `routineLogin()` API
   - 保存返回的 Token 到 Vuex 和缓存

2. **Token管理**：
   - Token存储在 `store.state.app.token`
   - 同时缓存到本地存储
   - 请求时自动添加到请求头：`Authori-zation: Bearer {token}`

3. **登录状态检测**：
   - 请求拦截器自动检测Token
   - Token过期自动跳转登录页
   - 状态码 `110002/110003/110004` 表示需要重新登录

### 权限控制

- **普通用户**：可访问大部分功能
- **管理员**：可访问 `managePages/` 下的管理功能
- **理事成员**：可查看合同、项目详情等

---

## 📡 API接口说明

### 接口基础信息

- **基础URL**：`https://test.acture-hub.com/api/` 或 `https://mini.acture-hub.com/api/`
- **请求方式**：GET/POST/PUT/DELETE
- **数据格式**：JSON
- **认证方式**：Bearer Token（除标注 `noAuth: true` 的接口）

### 核心业务接口

#### 1. 理事成员相关 (`api/gxhc.js`)
- `getDirectorMember()` - 获取理事成员列表
- `getDirectorDetails()` - 获取成员详情
- `directorMemberAdd()` - 添加成员（管理员）

#### 2. 项目相关 (`api/gxhc.js`)
- `investProjectsSave()` - 保存项目
- `investProjectsDetails()` - 获取项目详情
- `investProjectsUpdateBP()` - 更新BP
- `investProjectsUpdateSupply()` - 更新供需信息
- `investProjectsRevoke()` - 撤销项目

#### 3. BP诊断相关 (`api/gxhc.js`)
- `runBp()` - 运行BP诊断
- `getBpInfo()` - 获取BP信息
- `getBpResultList()` - 获取诊断结果列表
- `getBpResultInfo()` - 获取诊断结果详情
- `applyLiveApply()` - 申请直播诊断

#### 4. 能量系统 (`api/gxhc.js`)
- `userEnergy()` - 获取用户能量
- `energyList()` - 获取能量明细

#### 5. 用户相关 (`api/user.js`)
- `getUserInfo()` - 获取用户信息
- `updateUserInfo()` - 更新用户信息

#### 6. 公共接口 (`api/public.js`)
- `basicConfig()` - 获取基础配置
- `login()` - 小程序登录
- `routineBindingPhone()` - 绑定手机号
- `getColleges()` - 获取高校列表
- `getEnterprise()` - 获取企业列表

---

## 🎨 UI/UX设计

### 设计规范

- **主色调**：蓝色系（`#218DFF`）
- **背景色**：`#F6F6F6`
- **导航栏**：白色背景，黑色文字
- **TabBar**：底部三个Tab（首页、功能、我的）

### 组件使用

- 使用自定义组件库 `x-*` 系列
- 支持自定义导航栏（`navigationStyle: "custom"`）
- 使用 `mp-html` 组件渲染富文本内容

---

## 🚀 开发与部署

### 开发环境

1. **安装依赖**（如果有 package.json）：
   ```bash
   npm install
   ```

2. **运行项目**：
   - 使用 HBuilderX 打开项目
   - 选择运行到微信开发者工具
   - 或使用 `npm run dev:mp-weixin`

3. **环境配置**：
   - 修改 `config/app.js` 中的 `env` 变量
   - `test` - 测试环境
   - `mini` - 生产环境

### 构建发布

1. **配置小程序AppID**：
   - 在 `manifest.json` 中配置 `mp-weixin.appid`

2. **构建**：
   - 使用 HBuilderX 发行 -> 小程序-微信
   - 或使用命令行：`npm run build:mp-weixin`

3. **上传**：
   - 在微信开发者工具中上传代码
   - 提交审核

---

## 📝 注意事项

### 1. 代码规范
- 使用 Vue 3 Composition API（部分页面可能仍使用 Options API）
- 遵循 uni-app 开发规范
- 注意条件编译（`#ifdef MP`、`#ifdef H5` 等）

### 2. 性能优化
- 使用分包加载减少主包体积
- 图片资源使用云存储或CDN
- 列表使用虚拟滚动（如需要）

### 3. 兼容性
- 注意不同平台的API差异
- 使用条件编译处理平台差异
- 测试不同版本的微信小程序基础库

### 4. 安全
- Token不要明文存储（已使用缓存加密）
- 敏感操作需要二次确认
- API请求需要做好参数校验

---

## 🔍 关键文件说明

### 1. `App.vue`
- 应用入口文件
- 处理应用生命周期（onLaunch、onShow、onHide）
- 初始化基础配置
- 全局样式引入

### 2. `main.js`
- 应用主入口
- 初始化 Vue 应用
- 注册全局属性（$Cache、$util）
- 挂载 Vuex Store

### 3. `pages.json`
- 页面路由配置
- TabBar配置
- 分包配置
- 页面样式配置

### 4. `manifest.json`
- 应用配置清单
- 小程序AppID配置
- 权限配置
- 版本信息

---

## 📚 业务逻辑说明

### 1. 项目发起流程
1. 用户进入"发起项目"页面
2. 填写项目基本信息
3. 上传商业计划书（BP）
4. 提交审核
5. 理事成员查看并评分
6. 项目状态更新（待审核/已通过/已拒绝）

### 2. BP诊断流程
1. 用户上传BP文件
2. 调用 `runBp()` API
3. 系统AI分析BP内容
4. 生成诊断报告（Base版/Plus版）
5. 用户查看报告
6. 可选择申请一对一直播诊断

### 3. 积分系统
- 用户通过完成任务获得积分
- 积分可用于兑换服务或参与排行榜
- 管理员可以手动登记积分

### 4. 能量系统
- 类似积分的另一种虚拟货币
- 用于某些特定功能的消耗
- 有详细的能量明细记录

---

## 🐛 常见问题

### 1. 登录问题
- 检查Token是否过期
- 确认API地址配置正确
- 查看网络请求是否正常

### 2. 分包加载失败
- 检查分包路径配置
- 确认分包大小不超过限制（2MB）
- 检查分包名称是否重复

### 3. 图片加载失败
- 检查图片路径是否正确
- 确认云存储配置
- 检查网络连接

---

## 📞 技术支持

如有问题，可查看：
- 项目代码注释
- API接口文档
- uni-app官方文档：https://uniapp.dcloud.net.cn/

---

## 📅 版本历史

- **v1.0.0** (当前版本)
  - 基础功能实现
  - BP诊断功能
  - 项目管理系统
  - 理事成员管理
  - 积分和能量系统
  - 校园合伙人功能

---

**文档更新时间**：2024年  
**维护人员**：开发团队

